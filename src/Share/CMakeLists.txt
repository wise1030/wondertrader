CMAKE_MINIMUM_REQUIRED(VERSION 3.0.0)
PROJECT(Share LANGUAGES CXX)

FILE(GLOB HDRS *.h *.hpp threadpool/*.hpp)

ADD_LIBRARY(Share INTERFACE)
TARGET_INCLUDE_DIRECTORIES(Share INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
